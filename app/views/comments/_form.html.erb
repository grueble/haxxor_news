<%= form_for comment, :url => commentable_comments_path(commentable) do |form| %>

  <fieldset>
    <%= form.text_area(:body, :cols => 80, :rows => 10) %>
  </fieldset>
      
  <fieldset>
    <%= form.submit "Add Comment", :class => 'btn btn-primary' %>
  </fieldset>
<% end %>
